# pve-01 setup

- run pve helper scripts:
    1. post install
    2. microcode

`apt update && apt full-upgrade -y`

- run: 
    1. 60-aliases.sh (debian version)
    2. setup-lvm.sh
    3. optimize_nvme.sh


