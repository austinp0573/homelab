# R9700 runs

## record

date:

host ROCm:

llama image:

model:

context:

prompt:

tokens per second:

VRAM used:

notes:

## first target

Run the same short prompt with the general model and the coder model at `CTX_SIZE=8192`.

Use the model that gives useful answers before chasing a larger context or a second service.
