# copy to .env and edit

COMPOSE_PROJECT_NAME=nft-gui
CONTAINER_NAME=nft-gui
RESTART_POLICY=unless-stopped

# bind localhost by default; use 0.0.0.0 only if you want lan access
HOST_BIND=127.0.0.1
HOST_PORT=8787
